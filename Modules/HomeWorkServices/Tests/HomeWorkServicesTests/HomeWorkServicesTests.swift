import XCTest
@testable import HomeWorkServices

final class HomeWorkServicesTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(HomeWorkServices().text, "Hello, World!")
    }
}
